* stats thought about having {:base-value :operations} in e.g. :entity/hp

* but we want ops/modifiers for also projectile-size, this,that
; so makes sense to save @ entity ?

* so keep for now like that?


