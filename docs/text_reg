
* 1. graphics/create-sprite -> :texture-region (hard to find -> namespaced keys :sprite/texture-region ?

    clojure.graphics.texture/sub-region

    assoc-dimensions -> region width/height

    draw-texture-region!

    ui/image->widget / image-button ?? using sprites ??? stupid
      TextureRegionDrawable.
      .getRegionWidth / getRegionHeight


* 2. tiled/static-tiled-map-tile (gdx/tmx-tiled-map "path" ) ?
* 3. VisImage.
