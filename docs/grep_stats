
1. rename stats to ':stats/foo'

2. do not access keys

3. @ effects.target.damage direct interop w. creature blub (events keys too abstract)
  -> no more tx/event tx/assoc tx/dissoc
    -> creature functions
      -> stats functions

4.  defcomponent cdq.entity.stats -> actually cdq.creature.stats

5. entity info window knows about info text foozbuz

6. stats tests check

7. defmethods & function on stats in different places
  also why entity rendering concerned or text ?
    or all together ?

8. 'OPTIONAL' mana etc. is horrible and confusing and everywhere (or stat 0 ... )
