
forge.stage

; interesting -> move to 'app' then or app into 'stage' ?
; lets see....
; minimal knowledge (where does gui-viewport fit in then !?)
; also getRoot & fix reflc.
; maybe forge.core or forge.app ?

also findByName then no ui dependency
and proxy Stage can be done differently ?
